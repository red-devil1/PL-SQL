-- Experiment 1: Using Variables and Expressions 

-- Write a PL/SQL program that declares two numeric variables, assigns values to them, 
calculates their sum, and prints the result.

set serveroutput on;
DECLARE

-- Declare two numeric variables
num1 NUMBER := 25;
num2 NUMBER := 35;

--Variable to store the sum
sum_result NUMBER;
BEGIN

-- Calculate the sum
sum_result := num1 + num2;

--Print the result
DBMS_OUTPUT.PUT_LINE('The sum of ' || num1 || ' and ' || num2  || ' is: ' || sum_result);
end;
/
